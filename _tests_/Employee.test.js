const employee = require ('../lib/employee.js');

describe('employee', () => {
    it ('should provide a string of information', () => {
        const name = `${name}`;
        const id = `${id}`;
        const email = `${email}`;
        const role = `${role}`;
    });
});