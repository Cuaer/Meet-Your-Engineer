//Name requisites
const fs = require ('fs');
const inquirer = require ('inquirer');

//Employee will have the following properties name, id (title), email,  

const employee = {
    name : "name",
    id: "id",
    email: "email",
    github: "github",
    role: "employee",
};


//employee.forEach (`${employee.data.name}`, `${employee.data.email}`, `${employee.data.github}`);

//Employee will have the following methods: getName, getId, getEmail, getRole (will return 'Employee').

// getName();

// getId();

// getEmail();



//getRole

function getRole() {
    console.log (employee.role)
    return
};

getRole();